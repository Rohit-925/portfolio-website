{
    "builds":[{
        "src": "Base/wsgi",
        "use": "@vercel/python",
        "config": {"maxLamdaSize":"15mb","runtime": "python3.12"}

    }],
    "routes":[
        {
            "src": "/(.*)",
            "dest": "Base/wsgi.py"
        }
    ]
}